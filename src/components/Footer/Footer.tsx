import { ContainerFooter, MainFooter } from "./styles";

export default function Footer() {
  return (
    <ContainerFooter>
      <MainFooter>
        <p>HC & RS</p>
      </MainFooter>
    </ContainerFooter>
  );
}
